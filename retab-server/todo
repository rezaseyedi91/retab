- create a query to update parents field for meiTags
- remove parentId from meiTags
- measure n attribute in client repeats 
- tuning courses xmldIds are regenerated
- body tag for a doc must remain the same;
- when a doc gets removed, remove the empty userEnocderHeaderPresets!
- 
- UNDO crap! work on newest undo bug!

---------use dbman to findout how we can connect tuning type (n, pname, oct, accid) to meiTag (xmlId...)