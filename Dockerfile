# Root Dockerfile is not used when docker-compose.yaml specifies build contexts.
# Client and server are built separately using retab-client/Dockerfile and retab-server/Dockerfile.
# See docker-compose.yaml for the full orchestration.
